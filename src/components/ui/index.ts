/**
 * UI primitives still used by the app.
 *
 * The app's design system lives in `src/components/scout/*`. The only
 * scaffold pieces the app still consumes are the `cn` class-merge helper
 * and the toast provider/hook.
 *
 * @example
 * import { cn, ToastProvider, useToast } from '../components/ui'
 */

export { cn } from './utils'
export { ToastProvider, useToast } from './Toast'
